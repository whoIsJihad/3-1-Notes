# Single Cycle ALU Operation

Tags: alu, cpu, performance

A typical ALU operation in the ATmega32, such as adding two registers, completes in a single clock cycle (T1). This cycle is composed of three main stages that happen in parallel:

1. Register Operands Fetch
    
2. ALU Operation Execute
    
3. Result Write Back
